
all: $(BINDIR)/$(PLATFORM)/003$(VERSION)$(EXESFX)

$(OBJDIR)/003/$(PLATFORM)/%$(VERSION).obj: %.c
	mkdir -p `dirname $@`
	$(WCL) -q $(CFLAGS) $< -c -fo=$@ -dGUI_`echo $(PLATFORM) | tr [:lower:] [:upper:]`

$(BINDIR)/$(PLATFORM)/003$(VERSION)$(LNKSFX): $(OBJDIR)/003/$(PLATFORM)/main$(VERSION).obj $(LIBS)
	mkdir -p `dirname $@`
	wlink option quiet file $< name $@ $(LDFLAGS)

