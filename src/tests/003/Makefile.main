
all: $(BINDIR)/$(PLATFORM)/003$(VERSION)$(EXESFX)

$(OBJDIR)/003/$(PLATFORM)/%$(VERSION)$(OBJSFX): %.c
	mkdir -p `dirname $@`
	$(CC) $(CFLAGS) $< -c $(CCOUT)$@ $(CCDEF)GUI_`echo $(PLATFORM) | tr [:lower:] [:upper:]`

$(BINDIR)/$(PLATFORM)/003$(VERSION)$(LNKSFX): $(OBJDIR)/003/$(PLATFORM)/main$(VERSION)$(OBJSFX) $(LIBS)
	mkdir -p `dirname $@`
	$(LD) $< $(LDOUT) $@ $(LDFLAGS)
	$(postprocess)

