
all: $(BINDIR)/$(PLATFORM)/004$(VERSION)$(EXESFX)

$(OBJDIR)/004/$(PLATFORM)/%$(VERSION).obj: %.c
	mkdir -p `dirname $@`
	$(WCL) -q $(CFLAGS) $< -c -fo=$@ -dGUI_`echo $(PLATFORM) | tr [:lower:] [:upper:]`

$(BINDIR)/$(PLATFORM)/004$(VERSION)$(LNKSFX): $(OBJDIR)/004/$(PLATFORM)/main$(VERSION).obj $(LIBS)
	mkdir -p `dirname $@`
	wlink option quiet file $< name $@ $(LDFLAGS)

